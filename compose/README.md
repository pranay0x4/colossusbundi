# Compose Directory

This directory is split by operational domain:

- `media-stack/`
- `monitoring/`
- `networking/`
- `ai-stack/`

The actual manifests are not committed yet. They need one more pass to remove secrets, private addresses, and machine-specific details before public export.

Until then, the system design and incident history are documented in [`docs/`](/Users/pranay/colossusbundi/docs).
