# Configs Directory

This directory is reserved for sanitized configuration exports such as:

- NGINX reverse proxy snippets
- tunnel examples
- service config fragments

These are intentionally not committed yet because the live configs still contain private hostnames, addresses, or environment-specific details that should be cleaned before publication.
